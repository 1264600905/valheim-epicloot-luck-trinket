# 更新日志

## 0.2.0

- 包名（GUID）统一为 `trigger.epicloot.luckytrinket`（旧配置自动迁移）。
- 物品描述与 README 改为英文优先（三语保留）。
- 护符注册为 Utility 类：可放入 ExtraSlots「饰品」槽、多份共存，不占游戏原版「护符」位。
- 材料过滤改为可调范围 `MaterialTolerance`（默认 1 = 低一级材料保留）。
- 固有幸运显示在 tooltip；护符不再带 EpicLoot 随机词条。
- DebugLog 默认关闭。

## 0.1.0

- 初版：
  - 新增物品「幸运护符」（Trinket 饰品槽，工作台配方，金色模型/图标）。
  - 护符初始未附魔：在 EpicLoot 附魔台附魔并选择品质；
    本 mod 将已附魔护符补入附魔列表，支持随时重新附魔升级品质。
  - 护符不带 EpicLoot 随机词条/装饰名（附魔只改变品质），
    tooltip 显示固有幸运行；自动清理旧数据中的随机词条。
  - 佩戴时提供固有幸运 +25（写入 el-luk，非词条，不占效果槽、不受附魔影响）。
  - 掉落生成结束时，品质低于门槛的物品被排除：
    装备与未鉴定装备始终过滤；带品质的材料（碎片石/魔法材料/符文）
    允许低于门槛 MaterialTolerance 级保留（默认 1 = 低一级的材料也保留）；
    普通无品质材料不受影响。
  - 门槛经玩家 ZDO 广播（装备/卸下/魔法数据变化/周期兜底时刷新）。
  - ExtraSlots「饰品」槽兼容：护符注册为 **Utility** 类（与虚空宝箱同类），
    可放入额外「饰品」槽、多份共存，不占用游戏原版「护符」（Trinket）位；
    门槛与幸运对任意「饰品」槽中的护符照常生效。不再注册自开专属槽。
  - 配置：Enabled / Range / LuckBonus / MaterialTolerance / DebugLog。

