# 更新日志

## 0.3.0

- 新增物品「强运护符」（GreatLuckTrinket）：
  - 工作台配方：幸运护符 ×1 + 铁锭 ×10（紫色模型/图标）。
  - 拥有幸运护符的全部效果：附近掉落品质下限（附魔选择品质，可重新附魔升级）。
  - 两枚护符**移除固有幸运加成**（原 +25 Luck），幸运只保留 EpicLoot 原生词条来源。
  - 两枚护符描述补充说明：**不会提升掉落物品品质，而是取消低品质物品掉落**。
  - 新增**穿戴装备注入**：佩戴后，你身上可附魔的穿戴装备会按比例追加进附近掉落池，
    可以掉到自己穿的装备；原掉落池条目不受影响（不屏蔽任何掉落）。
  - 穿戴注入跳过不能附魔的物品（弹药、可堆叠物、受限物品等），避免普通物品频繁掉落。
  - 穿戴名单经玩家 ZDO（`ltr-worn`）广播，联机/单机均生效；多佩戴者取并集（同名取最大权重）。
  - 注入占比：注入组总权重 = 原池总权重 × `WornPoolShare`/(1-Share)，默认 0.2 → 我们的装备约 20%、原版池约 80%。
  - 注入组内相对权重：默认 1/件；**手持武器 2**（GetCurrentWeapon 判定）；
    **饰品（Utility 类型）0.4**（仅在安装 ExtraSlots 时生效，未安装则用默认权重）。
  - 注入装备品质**完全沿用原池分布**（不钳制、不提升），EpicLoot 原始品质几率保持不变；
    低于门槛的注入装备与其它掉落一样被门槛剔除（丢弃而非升级）。
  - 新增配置 `WornPool`（默认 true）、`WornPoolShare`（默认 0.2）、`WornPoolWeight`（默认 1）、
    `WornPoolHeldWeaponWeight`（默认 2）、`WornPoolUtilityWeight`（默认 0.4）。
  - 掉落诊断日志（DebugLog=true 时）：敌人掉落数量几率、四类掉落权重、每件实际分类（含穿戴注入标注与权重）、注入占比与过滤明细。

## 0.2.2

- 模组名称统一为 `EpicLoot_LuckTrinket`（体现与 EpicLoot 的强关联），游戏内显示名 "EpicLoot Luck Trinket"。
- 仓库更名为 valheim-epicloot-luck-trinket。

## 0.2.1

- 模组图标（icon.png）更换为游戏内护符的真实物品图标。

## 0.2.0

- 包名（GUID）统一为 `trigger.epicloot.luckytrinket`（旧配置自动迁移）。
- 物品描述与 README 改为英文优先（三语保留）。
- 护符注册为 Utility 类：可放入 ExtraSlots「饰品」槽、多份共存，不占游戏原版「护符」位。
- 材料过滤改为可调范围 `MaterialTolerance`（默认 1 = 低一级材料保留）。
- 固有幸运显示在 tooltip；护符不再带 EpicLoot 随机词条。
- DebugLog 默认关闭。

## 0.1.0

- 初版：
  - 新增物品「幸运护符」（Trinket 饰品槽，工作台配方，金色模型/图标）。
  - 护符初始未附魔：在 EpicLoot 附魔台附魔并选择品质；
    本 mod 将已附魔护符补入附魔列表，支持随时重新附魔升级品质。
  - 护符不带 EpicLoot 随机词条/装饰名（附魔只改变品质），
    tooltip 显示固有幸运行；自动清理旧数据中的随机词条。
  - 佩戴时提供固有幸运 +25（写入 el-luk，非词条，不占效果槽、不受附魔影响）。
  - 掉落生成结束时，品质低于门槛的物品被排除：
    装备与未鉴定装备始终过滤；带品质的材料（碎片石/魔法材料/符文）
    允许低于门槛 MaterialTolerance 级保留（默认 1 = 低一级的材料也保留）；
    普通无品质材料不受影响。
  - 门槛经玩家 ZDO 广播（装备/卸下/魔法数据变化/周期兜底时刷新）。
  - ExtraSlots「饰品」槽兼容：护符注册为 **Utility** 类（与虚空宝箱同类），
    可放入额外「饰品」槽、多份共存，不占用游戏原版「护符」（Trinket）位；
    门槛与幸运对任意「饰品」槽中的护符照常生效。不再注册自开专属槽。
  - 配置：Enabled / Range / LuckBonus / MaterialTolerance / DebugLog。

